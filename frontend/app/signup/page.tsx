import { Component as SignupPage } from "@/components/ui/animated-characters-signup-page";

export const metadata = {
  title: "Sign Up — OU Roundnet Club",
};

export default function SignupRoute() {
  return <SignupPage />;
}
