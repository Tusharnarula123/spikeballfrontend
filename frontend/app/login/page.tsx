import { Component as LoginPage } from "@/components/ui/animated-characters-login-page";

export const metadata = {
  title: "Sign In — OU Roundnet Club",
};

export default function LoginRoute() {
  return <LoginPage />;
}
